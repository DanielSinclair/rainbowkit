---
title: 在您的 dApp 中採用彩虹按鈕
description: 一個即插即用的 Wagmi 庫，用於在您的 dApp 中支持彩虹錢包
image: guide-rainbow-button.png
---

# 在您的 dApp 中採用彩虹按鈕

`RainbowButton` 組件是為使用 `wagmi` 的 dApp 添加對彩虹錢包支持的最簡單方法，並且比 [RainbowKit](https://www.rainbowkit.com/docs/installation) 提供更定制的連接體驗。

**1. 安裝 `@rainbow-me/rainbow-button` 及其對等的依賴項**

此包兼容 Next.js、React 和 Vite。 確保您遵循對等依賴項警告。

```bash
npm install @rainbow-me/rainbow-button wagmi viem@2.x @tanstack/react-query
```

**2. 配置 Wagmi 並安裝 RainbowButton**

將 `rainbowConnector` 的實例傳遞給 Wagmi 的 `createConfig`，並提供 `projectId` 和 `appName`，然後將您的應用包裹在 `RainbowButtonProvider` 中。 然後嵌入 `RainbowButton` 組件。

```tsx
import '@rainbow-me/rainbow-button/styles.css';
import {
  RainbowButtonProvider,
  RainbowButton,
  rainbowConnector,
} from '@rainbow-me/rainbow-button';
import { WagmiProvider, createConfig, http } from 'wagmi';
import { mainnet } from 'viem/chains';
import {
  QueryClientProvider,
  QueryClient,
} from "@tanstack/react-query";

const config = createConfig({
  connectors: [
    rainbowConnector({
      appName: 'RainbowKit demo',
      projectId: 'YOUR_PROJECT_ID',
    }),
  ],
  chains: [mainnet],
  transports: {
    [mainnet.id]: http(),
  },
  ssr: true,
});

const queryClient = new QueryClient();

function MyApp({ Component, pageProps }: AppProps) {
  return (
    <WagmiProvider config={config}>
      <QueryClientProvider client={queryClient}>
        <RainbowButtonProvider>
          <RainbowButton />
        </RainbowButtonProvider>
      </QueryClientProvider>
    </WagmiProvider>
  );
}
```

您還可以使用 `RainbowButton.Custom` 組件進行自定義實現和樣式。

```tsx
<RainbowButton.Custom>
  {({ ready, connect }) => {
    return (
      <button
        type="button"
        disabled={!ready}
        onClick={connect}
      >
        Connect Rainbow
      </button>
    );
  }}
</RainbowButton.Custom>
```

**3. 就是這樣!**

現在您的用戶可以無縫享受彩虹的連接體驗，無需任何維護或頭痛。

如果您想採用對其他錢包的支持，[RainbowKit](https://www.rainbowkit.com/docs/installation) 也提供了一個 [`WalletButton`](https://www.rainbowkit.com/docs/wallet-button) 組件。
