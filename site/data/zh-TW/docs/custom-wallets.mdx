---
title: 自定義錢包
description: 創建一個自定義錢包
---

# 自定義錢包

## 創建一個自定義錢包

> 注意：此 API 不穩定，可能在不久的將來會更改。 我們將隨時間添加更多內建錢包。 [讓我們知道](https://github.com/rainbow-me/rainbowkit/issues)是否有任何您感興趣的特定錢包。

`Wallet` 函數類型用於幫助您定義自己的自定義錢包。 以下屬性可以配置在您的 `Wallet` 函數的返回值上：

### 錢包屬性

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: '每個錢包的唯一 ID',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: '人類可讀的錢包名稱',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: '支持 EIP6963 的錢包的 RDNS',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: '移動使用的可選短名稱',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description: '錢包圖標的 URL，或解析為 Base64 數據 URL 的 Promise',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description: '用於擁有瀏覽器擴展和移動應用的錢包的下載流程中的強調色彩',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: '載入錢包圖標時的背景色',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          是否知道錢包已經安裝，或 <code>undefined</code> 如果無法確定
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: '包含下載 URL 的對象',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description: '計算是否應從列表中隱藏此錢包的函數。',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description: '提供連接器實例和不同連接方法配置的函數，詳見下方',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: '解析移動錢包連接 URI 的函數',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: '解析桌面應用深鏈接的函數',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description: '包含解析 QR 碼 URI 的函數的對象，加上可選的移動錢包設置說明',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description: '包含可選瀏覽器擴展設置說明的對象',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: '提供連接器實例的函數',
    },
  ]}
/> 這對於定義通用備用錢包非常有用，例如內建的“Injected Wallet”備用使用了此特性。

### 下載網址屬性

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'Google Play URL',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'Apple App Store URL',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        '當 `android` 或 `ios` 不可用時移動用戶的登錄頁面',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        '掃描移動下載 QR 碼的用戶的登錄頁面',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'Chrome Web Store URL',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'Microsoft Edge 附加組件 URL',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'Firefox 瀏覽器附加組件 URL',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'Opera 附加組件 URL',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'Mac App Store URL',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        '當瀏覽器兼容的 URL 不可用時桌面擴展用戶的登錄頁面',
    },
  ]}
/>

### 範例

例如，要使用 WalletConnect 創建一個自定義錢包：

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

然後您可以將您的自定義錢包添加到[錢包列表](/docs/custom-wallet-list)中。
