---
title: 自定 ConnectButton
description: 創建自定 ConnectButton
---

# 自定 ConnectButton

## 創建自定 ConnectButton

您可以使用低階的 `ConnectButton.Custom` 來創建自己自定的連接按鈕。 此組件會渲染一個函數，其中包含重新實現內建按鈕所需的一切。

內建按鈕的最小重新實現看起來大概是這樣的：

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

以下屬性將傳遞到您的渲染函數。

### 帳戶屬性

<PropsTable
  data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description:
        '包含當前帳戶詳細資訊的物件，詳見下文',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          完整的帳戶位址，例如
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: '小數位的帳戶餘額',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          格式化為字串的帳戶餘額，例如{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          帳戶餘額的貨幣符號，例如 <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          格式化為 3 個有效數字的餘額，加上符號，例如 <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          ENS 名稱，或截斷版本的位址，例如{' '}
          <code>"rainbowwallet.eth"</code> 或 <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'ENS 頭像的 URI',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          ENS 名稱，例如 <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description:
        '布林值，指示帳戶是否有當前鏈的待處理交易',
    },
  ]}
/>

### 鏈屬性

<PropsTable
  data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description:
        '包含當前鏈詳細資訊的物件，詳見下文',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: '鏈是否有指定的圖示',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description:
        '鏈圖標的 URL（下載 Base64 資料 URL 時也可能未定義）',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description:
        '圖像加載時可見的鏈圖標背景',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (<b>(</b>
        <>
          鏈 ID，例如 <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          鏈名稱，例如 <code>\"Ethereum\"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description:
        '布林值，指示當前鏈是否不被支持',
    },
  ]}
/>

### 模態狀態屬性

<PropsTable
  data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: '函數以開啟帳戶模態框',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: '函數以開啟鏈模態框',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: '函數以開啟連接模態框',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description:
        '布林值，指示帳戶模態框是否開啟',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: '布林值，指示鏈模態框是否開啟',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description:
        '布林值，指示連接模態框是否開啟',
    },
  ]}
/>

### 常規狀態屬性

<PropsTable
  data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description:
        '布林值，指示組件是否已掛載',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description:
        '驗證狀態，或如果未配置驗證則為 undefined',
    },
  ]}
/>
