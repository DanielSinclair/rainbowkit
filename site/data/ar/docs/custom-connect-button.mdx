---
title: زر الاتصال المخصص
description: إنشاء زر اتصال مخصص
---

# زر الاتصال المخصص

## إنشاء زر اتصال مخصص

يمكنك استخدام `ConnectButton.Custom` منخفض المستوى لإنشاء زر الاتصال المخصص الخاص بك. هذا المكون يجسد وظيفة، والتي تتضمن كل ما تحتاجه لإعادة تنفيذ الأزرار المدمجة.

قد تبدو إعادة تنفيذ بسيطة للأزرار المدمجة مثل هذا:

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

الخصائص التالية تُمرر إلى دالة العرض الخاصة بك.

### خصائص الحساب

<PropsTable
  data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description:
        'كائن يحتوي على تفاصيل حول الحساب الحالي، موضح أدناه',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          العنوان الكامل للحساب، على سبيل المثال.
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: 'رصيد الحساب بالأرقام العشرية',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          الرصيد الحسابي بصيغة سلسلة نصية، على سبيل المثال{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          رمز العملة للرصيد، على سبيل المثال <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          الرصيد بتنسيق 3 أرقام معنوية، مع الرمز
          على سبيل المثال <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          اسم ENS أو نسخة مختصرة من العنوان، على سبيل المثال{' '}
          <code>"rainbowwallet.eth"</code> أو <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'عنوان الصورة الرمزية لـ ENS',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          اسم ENS، على سبيل المثال <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description:
        'قيمة منطقية تشير إلى ما إذا كان لدى الحساب معاملات معلقة للسلسلة الحالية',
    },
  ]}
/>

### خصائص السلسلة

<PropsTable
  data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description:
        'كائن يحتوي على تفاصيل حول السلسلة الحالية، موضح أدناه',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: 'سواء كان للرمز سلسلة أيقونة محددة',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description:
        'عنوان URL لرمز السلسلة (والذي قد يكون غير معرف أثناء تنزيل عناوين URL لبيانات Base64)',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description:
        'خلفية رمز السلسلة التي ستكون مرئية أثناء تحميل الصور',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (
        <>
          معرف السلسلة، على سبيل المثال <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          اسم السلسلة، على سبيل المثال <code>"Ethereum"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description:
        'قيمة منطقية تشير إلى ما إذا كانت السلسلة الحالية غير مدعومة',
    },
  ]}
/>

### خصائص حالة النموذج

<PropsTable
  data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: 'دالة لفتح نافذة حساب',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: 'دالة لفتح نافذة سلسلة',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: 'دالة لفتح نافذة الاتصال',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description:
        'قيمة منطقية تشير إلى ما إذا كانت نافذة الحساب مفتوحة',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: 'قيمة منطقية تشير إلى ما إذا كانت نافذة السلسلة مفتوحة',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description:
        'قيمة منطقية تشير إلى ما إذا كانت نافذة الاتصال مفتوحة',
    },
  ]}
/>

### خصائص الحالة العامة

<PropsTable
  data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description:
        'قيمة منطقية تشير إلى ما إذا كان المكون قد تم تحميله',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description:
        'حالة المصادقة، أو غير معرّف إذا لم يتم تكوين المصادقة',
    },
  ]}
/>
