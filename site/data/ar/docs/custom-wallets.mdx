---
title: محافظ مخصصة
description: إنشاء محفظة مخصصة
---

# محافظ مخصصة

## إنشاء محفظة مخصصة

> ملاحظة: هذا الواجهة البرمجية للتطبيقات غير مستقرة ومرجح أن تتغير في المستقبل القريب. سنقوم بإضافة المزيد من المحافظ المدمجة مع مرور الوقت. [أعلمنا](https://github.com/rainbow-me/rainbowkit/issues) إذا كانت هناك أي محافظ معينة تهمك.

يتم توفير نوع الوظيفة `Wallet` لمساعدتك في تعريف محافظك المخصصة. يمكن تكوين الخصائص التالية على قيمة الإرجاع لوظيفة `Wallet` الخاصة بك:

### خصائص المحفظة

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'معرّف فريد لكل محفظة',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'اسم المحفظة بشكل قابل للقراءة البشرية',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS للمحفظة التي تدعم EIP6963',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'اسم مختصر اختياري للاستخدام المحمول',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'رابط URL لأيقونة المحفظة، أو وعد ينتهي برابط بيانات Base64',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'لون التمييز المستخدم في عمليات تحميل المحافظ التي تحتوي على امتداد متصفح وتطبيق محمول',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'لون الخلفية أثناء تحميل أيقونة المحفظة',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          سواء كانت المحفظة معروفة بأنه تم تثبيتها، أو{' '}
          <code>undefined</code> إذا كانت غير محددة
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'كائن يحتوي على عناوين URL للتنزيل',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'وظيفة لحساب ما إذا كان ينبغي إخفاء هذه المحفظة من القائمة.',
    } 
  ]}
/> هذا مفيد لتحديد محافظ الاسترداد العامة، على سبيل المثال فإن خاصية الاسترداد "Injected Wallet" المدمجة تستخدم هذه الميزة',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'وظيفة لتوفير مثيل الموصل وتكوين طرق الاتصال المختلفة، الموضحة أدناه',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        'وظيفة لحل معرّف الاتصال لمحفظة المحمول',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: 'وظيفة لحل رابط تطبيق سطح المكتب العمق',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'كائن يحتوي على وظيفة لحل رابط رمز الاستجابة السريعة، بالإضافة إلى تعليمات إعداد محفظة المحمول الاختيارية',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'كائن يحتوي على تعليمات إعداد امتداد المستعرض الاختيارية',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: 'وظيفة لتوفير مثيل الموصل',
    }]}

### خصائص روابط التحميل

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'رابط Google Play',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'رابط Apple App Store',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'صفحة الهبوط لمستخدمي الجوال عند عدم توفر `android` أو `ios`',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'صفحة الهبوط للمستخدمين الذين يقومون بمسح رمز الاستجابة السريعة لتحميل التطبيق',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'رابط Chrome Web Store',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'رابط إضافات Microsoft Edge',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'رابط إضافات متصفح Firefox',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'رابط إضافات Opera',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'رابط Mac App Store',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'صفحة الهبوط لمستخدمي ملحقات سطح المكتب عند عدم توفر روابط متوافقة مع المتصفح',
    },
  ]}
/>

### أمثلة

مثلا، لإنشاء محفظة مخصصة باستخدام WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

يمكنك بعد ذلك إضافة محفظتك المخصصة في [قائمة المحفظة](/docs/custom-wallet-list).
