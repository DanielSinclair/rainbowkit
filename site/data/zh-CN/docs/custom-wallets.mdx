---
title: 自定义钱包
description: 创建自定义钱包
---

# 自定义钱包

## 创建自定义钱包

> 注意：此API不稳定，很可能在不久的将来发生变化。 我们将随着时间推移增加更多内置钱包。 如果您对任何特定的钱包有兴趣，[请告诉我们](https://github.com/rainbow-me/rainbowkit/issues)。

`钱包`函数类型提供帮助你定义自己的自定义钱包。 您的`Wallet`函数返回值上可以配置以下属性：

### 钱包属性

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: '每个钱包的唯一ID',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: '人类可读的钱包名称',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: '支持EIP6963的钱包的RDNS',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: '用于移动设备的可选短名称',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        '钱包图标的URL，或一个解析为Base64数据URL的Promise',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        '用于拥有浏览器扩展和移动应用的钱包的下载流程中的强调色',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: '钱包图标加载时的背景色',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          钱包是否已知安装，或 <code>undefined</code> 如果不可确定
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: '包含下载URL的对象',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        '计算是否应将此钱包从列表中隐藏的函数，这对于定义通用回退钱包很有用，例如内置的“Injected Wallet”回退就是利用此功能',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        '提供不同连接方法的连接器实例和配置的函数，详述如下',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        '解析移动钱包连接 URI 的函数',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: '解析桌面应用深层链接的函数',
    },
    {
      name: 'qrCode',
      required: false,
      type: '{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: \'install\' | \'create\' | \'scan\', title: string, description: string }> }}}',
      typeSimple: 'object',
      description:
        '包含解析二维码 URI 的函数的对象，以及可选的移动钱包设置说明',
    },
    {
      name: 'extension',
      required: false,
      type: '{ instructions?: { learnMoreUrl: string, steps: Array<{ step: \'install\' | \'create\' | \'scan\', title: string, description: string }> }}}',
      typeSimple: 'object',
      description:
        '包含可选浏览器扩展设置说明的对象',
    },
    {
      name: 'createConnector',
      required: true,
      type: 'createConnector: (details: WalletDetailsParams) => CreateConnectorFn',
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: '提供连接器实例的函数',
    },
  ]}
/> 下载链接属性

### 下载链接属性

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'Google Play URL',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'Apple App Store URL',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        '当`android`或`ios`不可用时，移动用户的登录页面',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        '扫描移动下载二维码的用户的登录页面',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'Chrome Web Store URL',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'Microsoft Edge 附加组件 URL',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'Firefox 浏览器附加组件 URL',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'Opera 附加组件 URL',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'Mac App Store URL',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        '当浏览器兼容的 URL 不可用时，桌面扩展用户的登录页面',
    },
  ]}
/>

### 示例

例如，要使用 WalletConnect 创建一个自定义钱包：

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

然后，您可以在[钱包列表](/docs/custom-wallet-list)中添加您的自定义钱包。
