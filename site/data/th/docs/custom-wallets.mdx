---
title: กระเป๋าเงินที่กำหนดเอง
description: สร้างกระเป๋าเงินที่กำหนดเอง
---

# กระเป๋าเงินที่กำหนดเอง

## สร้างกระเป๋าเงินที่กำหนดเอง

> หมายเหตุ: API นี้ไม่เสถียรและมีความเป็นไปได้ที่จะเปลี่ยนแปลงในอนาคตใกล้. เราจะเพิ่มกระเป๋าเงินในตัวมากขึ้นตามเวลา. [แจ้งให้เรารู้](https://github.com/rainbow-me/rainbowkit/issues) หากมีกระเป๋าเงินเฉพาะที่คุณสนใจ.

ฟังก์ชันประเภท `Wallet` ได้รับการให้เพื่อช่วยคุณกำหนดกระเป๋าเงินที่กำหนดเองของคุณเอง. คุณสามารถกำหนดค่าทรัพย์สินต่อไปนี้บนค่าที่คืนกลับของฟังก์ชัน `Wallet` ของคุณ:

### คุณสมบัติของ Wallet

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'รหัสเฉพาะสำหรับแต่ละกระเป๋าเงิน',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'ชื่อกระเป๋าเงินที่มนุษย์สามารถอ่านได้',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS สำหรับกระเป๋าเงินที่รองรับ EIP6963',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'ชื่อย่อที่ไม่จำเป็นสำหรับการใช้งานบนมือถือ',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL สำหรับไอคอนกระเป๋าเงิน หรือ promise ที่ส่งผลไปยัง URL ข้อมูล Base64',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'สีที่ใช้เน้นในขั้นตอนการดาวน์โหลดสำหรับกระเป๋าเงินที่มีทั้งโปรแกรมเสริมของเบราว์เซอร์และแอปมือถือ',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'สีพื้นหลังขณะไอคอนกระเป๋าเงินกำลังโหลด',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          กระเป๋าเงินถูกติดตั้งอยู่หรือไม่, หรือ{' '}
          <code>undefined</code> ถ้าไม่สามารถระบุได้
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'วัตถุที่มี URL สำหรับการดาวน์โหลด',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'ฟังก์ชันสำหรับคำนวณว่ากระเป๋าเงินนี้ควรจะถูกซ่อนจากรายการหรือไม่ มีประโยชน์สำหรับการกำหนดกระเป๋าเงินสำรองทั่วไป เช่น กระเป๋าเงินสำรองในตัว "Injected Wallet" ใช้คุณสมบัตินี้
 },
 {
 name: 'createConnector',
 required: true,
 type: '() => RainbowKitConnector',
 typeSimple: 'RainbowKitConnector',
 description:
 'ฟังก์ชันสำหรับการให้ตัวอย่างการเชื่อมต่อและการกำหนดค่าสำหรับวิธีการเชื่อมต่อที่แตกต่างกันที่อธิบายด้านล่าง',
 },
 {
 name: 'mobile',
 required: false,
 type: '{ getUri?: (uri: string) => string }',
 typeSimple: 'object',
 description:
 'ฟังก์ชันสำหรับการแก้ไข URI การเชื่อมต่อกระเป๋าเงินมือถือ',
 },
 {
 name: 'desktop',
 required: false,
 type: '{ getUri?: (uri: string) => string }',
 typeSimple: 'object',
 description: 'ฟังก์ชันสำหรับการแก้ไขลิงก์แอปพลิเคชันบนเดสก์ท็อป',
 },
 {
 name: 'qrCode',
 required: false,
 type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
 typeSimple: 'object',
 description:
 'ออบเจ็กต์ที่มีฟังก์ชันสำหรับการแก้ไข URI ของ QR code และคำแนะนำการตั้งค่ากระเป๋าเงินมือถือเพิ่มเติม',
 },
 {
 name: 'extension',
 required: false,
 type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
 typeSimple: 'object',
 description:
 'ออบเจ็กต์ที่มีคำแนะนำการติดตั้งส่วนขยายเบราว์เซอร์เพิ่มเติม',
 },
 {
 name: 'createConnector',
 required: true,
 type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
 typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
 description: 'ฟังก์ชันสำหรับการให้ตัวอย่างการเชื่อมต่อ',
 },
 ]}
/>

### คุณสมบัติของ DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'URL Google Play',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'URL Apple App Store',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'หน้าลงจอดสำหรับผู้ใช้มือถือเมื่อ `android` หรือ `ios` ไม่พร้อมใช้งาน',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'หน้าลงจอดสำหรับผู้ใช้ที่สแกน QR Code เพื่อดาวน์โหลดแอปมือถือ',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'URL Chrome Web Store',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'URL ส่วนขยาย Microsoft Edge',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'URL ส่วนขยายของ Firefox Browser',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'URL ส่วนขยายของ Opera',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'URL ของ Mac App Store',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'หน้าลงจอดสำหรับผู้ใช้ส่วนขยายเดสก์ท็อปเมื่อ URL ที่รองรับเบราว์เซอร์ไม่พร้อมใช้งาน',
    },
  ]}
/>

### ตัวอย่าง

เช่น เพื่อสร้างกระเป๋าเงินที่กำหนดเองโดยใช้ WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

คุณสามารถเพิ่มกระเป๋าเงินที่กำหนดเองของคุณใน[รายการกระเป๋าเงิน](/docs/custom-wallet-list).
