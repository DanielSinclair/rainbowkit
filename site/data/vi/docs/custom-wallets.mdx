---
title: Ví tùy chỉnh
description: Tạo ví tùy chỉnh
---

# Ví tùy chỉnh

## Tạo ví tùy chỉnh

> Lưu ý: API này không ổn định và có khả năng thay đổi trong tương lai gần. Chúng tôi sẽ thêm nhiều ví tích hợp hơn theo thời gian. [Hãy cho chúng tôi biết](https://github.com/rainbow-me/rainbowkit/issues) nếu có bất kỳ ví cụ thể nào bạn quan tâm.

Loại hàm `Wallet` được cung cấp để giúp bạn xác định ví tùy chỉnh của riêng mình. Các thuộc tính sau có thể được cấu hình trên giá trị trả về của hàm `Wallet` của bạn:

### Thuộc tính ví

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'ID duy nhất cho mỗi ví.',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Tên ví dễ đọc cho người dùng.',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS cho ví hỗ trợ EIP6963.',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Tên ngắn tùy chọn cho việc sử dụng di động.',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL cho biểu tượng ví, hoặc một promise giải quyết thành URL dữ liệu Base64.',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Màu nhấn sử dụng trong các quy trình tải xuống cho ví có cả tiện ích mở rộng trình duyệt và ứng dụng di động.',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Màu nền trong khi tải biểu tượng ví.',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Liệu ví có được biết là đã cài đặt hay không, hoặc{' '}
          <code>undefined</code> nếu không xác định.
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Đối tượng chứa các URL tải xuống.',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Hàm tính toán xem ví này có nên bị ẩn khỏi danh sách không. Điều này hữu ích cho việc xác định các ví dự phòng chung, ví dụ như ví dự phòng "Ví tiêm" tích hợp sử dụng tính năng này'.
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Chức năng cung cấp phiên bản và cấu hình connector cho các phương thức kết nối khác nhau, được mô tả dưới đây',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        'Chức năng để giải quyết URI kết nối ví di động',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: 'Chức năng để giải quyết liên kết sâu ứng dụng trên máy tính để bàn',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Đối tượng chứa chức năng để giải quyết URI mã QR, cộng với hướng dẫn cài đặt ví di động tùy chọn',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Đối tượng chứa hướng dẫn cài đặt tiện ích trình duyệt tùy chọn',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: 'Chức năng để cung cấp phiên bản connector',
    }
  ]}
/>

### Thuộc tính DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'Google Play URL',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'Apple App Store URL',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Landing page for mobile users when `android` or `ios` are unavailable',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Landing page for users that scan the mobile download QR Code',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'Chrome Web Store URL',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'Microsoft Edge Add-ons URL',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'Firefox Browser Add-ons URL',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'Opera add-ons URL',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'Mac App Store URL',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Landing page for desktop extension users when browser-compatible URLs are unavailable',
    },
  ]}
/>

### Ví dụ

Ví dụ, để tạo ví tùy chỉnh sử dụng WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

Bạn có thể thêm ví tùy chỉnh của mình vào [danh sách ví](/docs/custom-wallet-list).
