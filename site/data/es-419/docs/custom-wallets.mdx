---
title: Carteras Personalizadas
description: Crear una billetera personalizada
---

# Carteras Personalizadas

## Crear una billetera personalizada

> Nota: Esta API es inestable y es probable que cambie en un futuro cercano. Agregaremos más billeteras incorporadas con el tiempo. [Háganos saber](https://github.com/rainbow-me/rainbowkit/issues) si hay alguna billetera particular que le interese.

El tipo de función `Wallet` se proporciona para ayudarlo a definir sus propias billeteras personalizadas. Las siguientes propiedades se pueden configurar en el valor de retorno de su función `Wallet`:

### Propiedades de la Billetera

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'ID único por billetera',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Nombre legible por humanos de la billetera',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS para billetera que soporta EIP6963',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Nombre corto opcional para uso móvil',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL para el icono de la billetera, o una promesa que resuelve a una URL de datos en Base64.',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Color de acento utilizado en los flujos de descarga para billeteras con una extensión de navegador y una aplicación móvil.',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Color de fondo mientras se carga el ícono de la billetera.',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Si se sabe que la billetera está instalada, o{' '}
          <code>undefined</code> si no se puede determinar.
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Objeto que contiene URLs de descarga.',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Función para calcular si esta billetera debe ocultarse de la lista. Esto es útil para definir billeteras de respaldo genéricas, por ejemplo, el respaldo incorporado de "Billetera Inyectada" hace uso de esta función.',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Función para proporcionar la instancia del conector y la configuración para diferentes métodos de conexión, descritos a continuación.',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        'Función para resolver un URI de conexión de billetera móvil.',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: 'Función para resolver un enlace profundo de la aplicación de escritorio.',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objeto que contiene una función para resolver el URI del código QR, además de instrucciones adicionales para configurar la billetera móvil.',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objeto que contiene instrucciones opcionales para configurar la extensión del navegador.',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: 'Función para proporcionar la instancia del conector.',
    },
  ]}
/> Esto es útil para definir billeteras de respaldo genéricas, por ejemplo, el respaldo integrado de "Billetera Inyectada" hace uso de esta función.

### Propiedades de DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'URL de Google Play',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'URL de Apple App Store',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Página de aterrizaje para usuarios móviles cuando `android` o `ios` no están disponibles',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Página de aterrizaje para usuarios que escanean el código QR de descarga móvil',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'URL de Chrome Web Store',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'URL de Microsoft Edge Add-ons',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'URL de complementos de Firefox',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'URL de complementos de Opera',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'URL de Mac App Store',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Página de aterrizaje para usuarios de extensión de escritorio cuando las URLs compatibles con el navegador no están disponibles',
    },
  ]}
/>

### Ejemplos

Por ejemplo, para crear una billetera personalizada utilizando WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

Luego puedes agregar tu billetera personalizada en la [lista de billeteras](/docs/custom-wallet-list).
