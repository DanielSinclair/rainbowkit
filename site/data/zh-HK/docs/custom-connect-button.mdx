---
title: 自定 ConnectButton
description: 創建自定 ConnectButton
---

# 自定 ConnectButton

## 創建自定 ConnectButton

你可以使用低層的 `ConnectButton.Custom` 來創建自己的連接按鈕。 這個元件會渲染一個函數，包含了重新實現內置按鈕所需的一切。

內置按鈕的最小重新實現應該會像這樣：

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

以下的參數會傳遞給你的渲染函數。

### 帳戶屬性

<PropsTable data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description: '包含目前帳戶詳細資訊的物件，詳情如下',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          完整的帳戶地址，例如
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: '帳戶餘額的小數位數',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          帳戶餘額格式為字串，例如{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          帳戶餘額的貨幣符號，例如 <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          帳戶餘額格式化到3個有效位數，加上
          符號，例如 <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          ENS 名稱或者截斷的地址，例如{' '}
          <code>"rainbowwallet.eth"</code> 或 <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'ENS 頭像的 URI',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          ENS 名稱，例如 <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description: '布林值，指出該帳戶是否在當前鏈上有待處理的交易',
    },
  ]} />

### 鏈屬性

<PropsTable data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description: '包含當前鏈詳細資訊的物件，詳情如下',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: '鏈是否有指定的圖示',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description: '鏈圖示的 URL（在下載 Base64 資料 URL 時也可能為未定義）',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description: '圖示加載期間所見的背景色',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (
        <>
          鏈 ID，例如 <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          鏈名稱，例如 <code>"Ethereum"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description: '布林值，指出當前鏈是否不受支持',
    },
  ]} />

### 模式狀態屬性

<PropsTable data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: '打開帳戶模式的函數',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: '打開鏈模式的函數',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: '打開連接模式的函數',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description: '布林值，指出帳戶模式是否打開',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: '布林值，指出鏈模式是否打開',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description: '布林值，指出連接模式是否打開',
    },
  ]} />

### 常規狀態屬性

<PropsTable data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description: '布林值，指出組件是否已掛載',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description: '認證狀態，或者如果未配置認證則為 undefined',
    },
  ]} />
