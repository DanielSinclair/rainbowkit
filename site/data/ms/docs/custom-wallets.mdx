---
title: Dompet Tersuai
description: Buat dompet tersuai
---

# Dompet Tersuai

## Buat dompet tersuai

> Nota: API ini tidak stabil dan mungkin akan berubah dalam masa terdekat. Kami akan menambah lebih banyak dompet terbina dalam dari semasa ke semasa. [Beritahu kami](https://github.com/rainbow-me/rainbowkit/issues) jika terdapat mana-mana dompet tertentu yang anda minati.

Jenis fungsi `Wallet` disediakan untuk membantu anda mendefinisikan dompet khusus anda sendiri. Properti berikut boleh dikonfigurasi pada nilai balikan dari fungsi `Wallet` anda:

### Sifat Dompet

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'Unique ID per wallet',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Human-readable wallet name',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS for wallet which supports EIP6963',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Optional short name for mobile use',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description:
        'URL for wallet icon, or a promise that resolves to a Base64 data URL',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description:
        'Accent color used in download flows for wallets with both a browser extension and a mobile app',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Background color while the wallet icon loads',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Whether the wallet is known to be installed, or{' '}
          <code>undefined</code> if indeterminate
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Object containing download URLs',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description:
        'Function for calculating whether this wallet should be hidden from the list. Ini berguna untuk mentakrifkan dompet sandaran generik, contohnya, sandaran "Injected Wallet" terbina dalam menggunakan ciri ini',
    },
    {
      name: 'createConnector',
      required: true,
      type: '() => RainbowKitConnector',
      typeSimple: 'RainbowKitConnector',
      description:
        'Fungsi untuk menyediakan instance penyambung dan konfigurasi untuk pelbagai kaedah sambungan, diterangkan di bawah',
    },
    {
      name: 'mobile',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description:
        'Fungsi untuk menyelesaikan URI sambungan dompet mudah alih',
    },
    {
      name: 'desktop',
      required: false,
      type: '{ getUri?: (uri: string) => string }',
      typeSimple: 'object',
      description: 'Fungsi untuk menyelesaikan pautan mendalam aplikasi desktop',
    },
    {
      name: 'qrCode',
      required: false,
      type: "{ getUri: (uri: string) => string, instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objek yang mengandungi fungsi untuk menyelesaikan URI kod QR, serta arahan pemasangan dompet mudah alih pilihan',
    },
    {
      name: 'extension',
      required: false,
      type: "{ instructions?: { learnMoreUrl: string, steps: Array<{ step: 'install' | 'create' | 'scan', title: string, description: string }> }}}",
      typeSimple: 'object',
      description:
        'Objek yang mengandungi arahan pemasangan pelayar pilihan',
    },
    {
      name: 'createConnector',
      required: true,
      type: "createConnector: (details: WalletDetailsParams) => CreateConnectorFn",
      typeSimple: '(details: WalletDetailsParams) => CreateConnectorFn',
      description: 'Fungsi untuk menyediakan instance penyambung',
    },
  ]}]}

### Sifat DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'URL Google Play',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'URL Apple App Store',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Halaman pendaratan untuk pengguna mudah alih apabila `android` atau `ios` tidak tersedia',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Halaman pendaratan untuk pengguna yang mengimbas Kod QR muat turun mudah alih',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'URL Chrome Web Store',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'URL Tambahan Microsoft Edge',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'URL Tambahan Pelayar Firefox',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'URL Tambahan Opera',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'URL Mac App Store',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Halaman pendaratan untuk pengguna sambungan desktop apabila URL yang serasi dengan pelayar tidak tersedia',
    },
  ]}
/>

### Contoh-contoh

Sebagai contoh, untuk membuat dompet tersuai menggunakan WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

Anda boleh menambah dompet tersuai anda dalam [senarai dompet](/docs/custom-wallet-list).
