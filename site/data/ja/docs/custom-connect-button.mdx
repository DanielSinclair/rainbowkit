---
title: カスタムConnectButton
description: カスタムConnectButtonの作成
---

# カスタムConnectButton

## カスタムConnectButtonの作成

あなたは低レベルの`ConnectButton.Custom`を使用して、独自のカスタム接続ボタンを作成することができます。 このコンポーネントは、組み込みボタンを再実装するために必要なすべてを含む関数をレンダリングします。 このコンポーネントは、組み込みボタンを再実装するために必要なすべてを含む関数をレンダリングします。

組み込みボタンの最小の再実装は次のようになるでしょう：

```tsx
import { ConnectButton } from '@rainbow-me/rainbowkit';

export const YourApp = () => {
  return (
    <ConnectButton.Custom>
      {({
        account,
        chain,
        openAccountModal,
        openChainModal,
        openConnectModal,
        authenticationStatus,
        mounted,
      }) => {
        // Note: If your app doesn't use authentication, you
        // can remove all 'authenticationStatus' checks
        const ready = mounted && authenticationStatus !== 'loading';
        const connected =
          ready &&
          account &&
          chain &&
          (!authenticationStatus ||
            authenticationStatus === 'authenticated');

        return (
          <div
            {...(!ready && {
              'aria-hidden': true,
              'style': {
                opacity: 0,
                pointerEvents: 'none',
                userSelect: 'none',
              },
            })}
          >
            {(() => {
              if (!connected) {
                return (
                  <button onClick={openConnectModal} type="button">
                    Connect Wallet
                  </button>
                );
              }

              if (chain.unsupported) {
                return (
                  <button onClick={openChainModal} type="button">
                    Wrong network
                  </button>
                );
              }

              return (
                <div style={{ display: 'flex', gap: 12 }}>
                  <button
                    onClick={openChainModal}
                    style={{ display: 'flex', alignItems: 'center' }}
                    type="button"
                  >
                    {chain.hasIcon && (
                      <div
                        style={{
                          background: chain.iconBackground,
                          width: 12,
                          height: 12,
                          borderRadius: 999,
                          overflow: 'hidden',
                          marginRight: 4,
                        }}
                      >
                        {chain.iconUrl && (
                          <img
                            alt={chain.name ?? 'Chain icon'}
                            src={chain.iconUrl}
                            style={{ width: 12, height: 12 }}
                          />
                        )}
                      </div>
                    )}
                    {chain.name}
                  </button>

                  <button onClick={openAccountModal} type="button">
                    {account.displayName}
                    {account.displayBalance
                      ? ` (${account.displayBalance})`
                      : ''}
                  </button>
                </div>
              );
            })()}
          </div>
        );
      }}
    </ConnectButton.Custom>
  );
};
```

以下のpropsがあなたのレンダー関数に渡されます。

### アカウント属性

<PropsTable
  data={[
    {
      name: 'account',
      required: false,
      type: 'object | undefined',
      description:
        '以下に説明する現在のアカウントの詳細を含むオブジェクト',
    },
    {
      name: 'account.address',
      required: false,
      type: 'string',
      description: (
        <>
          アカウントの完全なアドレス。例:
          "0x7a3d05c70581bD345fe117c06e45f9669205384f"
        </>
      ),
    },
    {
      name: 'account.balanceDecimals',
      required: false,
      type: 'string | undefined	',
      description: 'アカウントの小数点以下の残高',
    },
    {
      name: 'account.balanceFormatted',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          文字列としてフォーマットされたアカウント残高。例:{' '}
          <code>1.234567890123456789</code>
        </>
      ),
    },
    {
      name: 'account.balanceSymbol',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          残高の通貨記号。例: <code>ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayBalance',
      required: false,
      type: 'string | undefined	',
      description: (
        <>
          3桁の有効数字と記号でフォーマットされた残高。例: <code>1.23 ETH</code>
        </>
      ),
    },
    {
      name: 'account.displayName',
      required: false,
      type: 'string',
      description: (
        <>
          ENS名、またはアドレスの省略バージョン。例:{' '}
          <code>"rainbowwallet.eth"</code> または <code>"0x7a…384f"</code>
        </>
      ),
    },
    {
      name: 'account.ensAvatar',
      required: false,
      type: 'string	| undefined',
      description: 'ENSアバターのURI',
    },
    {
      name: 'account.ensName',
      required: false,
      type: 'string	| undefined',
      description: (
        <>
          ENS名。例: <code>rainbowwallet.eth</code>
        </>
      ),
    },
    {
      name: 'account.hasPendingTransactions',
      required: false,
      type: 'boolean',
      description:
        '現在のチェーンに対する保留中のトランザクションがあるかどうかを示すブール値',
    },
  ]}
/>

### チェーン属性

<PropsTable
  data={[
    {
      name: 'chain',
      required: false,
      type: 'object | undefined',
      description:
        '現在のチェーンに関する詳細を含むオブジェクト。以下に説明。',
    },
    {
      name: 'chain.hasIcon',
      required: false,
      type: 'boolean',
      description: 'チェーンにアイコンが指定されているかどうか',
    },
    {
      name: 'chain.iconUrl',
      required: false,
      type: 'string | undefined',
      description:
        'チェーンアイコンのURL（Base64データURLをダウンロード中の場合も未定義の可能性あり）',
    },
    {
      name: 'chain.iconBackground',
      required: false,
      type: 'string | undefined',
      description:
        '画像が読み込まれる間に表示されるチェーンアイコンの背景',
    },
    {
      name: 'chain.id',
      required: false,
      type: 'number',
      description: (<b>(</b>
        <>
          チェーンID。例: <code>1</code>
        </>
      ),
    },
    {
      name: 'chain.name',
      required: false,
      type: 'string | undefined',
      description: (
        <>
          チェーン名。例: <code>"Ethereum"</code>
        </>
      ),
    },
    {
      name: 'chain.unsupported',
      required: false,
      type: 'boolean | undefined',
      description:
        '現在のチェーンがサポートされていないかどうかを示すブール値',
    },
  ]}
/>

### モーダル状態のプロパティ

<PropsTable
  data={[
    {
      name: 'openAccountModal',
      required: false,
      type: '() => void',
      description: 'アカウントモーダルを開くための関数',
    },
    {
      name: 'openChainModal',
      required: false,
      type: '() => void',
      description: 'チェーンモーダルを開くための関数',
    },
    {
      name: 'openConnectModal',
      required: false,
      type: '() => void',
      description: '接続モーダルを開くための関数',
    },
    {
      name: 'accountModalOpen',
      required: false,
      type: 'boolean',
      description:
        'アカウントモーダルが開いているかどうかを示すブール値',
    },
    {
      name: 'chainModalOpen',
      required: false,
      type: 'boolean',
      description: 'チェーンモーダルが開いているかどうかを示すブール値',
    },
    {
      name: 'connectModalOpen',
      required: false,
      type: 'boolean',
      description:
        '接続モーダルが開いているかどうかを示すブール値',
    },
  ]}
/>

### 一般的な状態プロパティ

<PropsTable
  data={[
    {
      name: 'mounted',
      required: false,
      type: 'boolean',
      description:
        'コンポーネントがマウントされたかどうかを示すブール値',
    },
    {
      name: 'authenticationStatus',
      required: false,
      type: '"loading" | "unauthenticated" | "authenticated" | undefined',
      description:
        '認証ステータス。認証が構成されていない場合は未定義',
    },
  ]}
/>
