---
title: Кастомні Гаманці
description: Створіть кастомний гаманець
---

# Кастомні Гаманці

## Створіть кастомний гаманець

> Примітка: Цей API нестабільний і, ймовірно, зміниться найближчим часом. З часом ми будемо додавати більше вбудованих гаманців. [Повідомте нам](https://github.com/rainbow-me/rainbowkit/issues), якщо вам цікаві певні гаманці.

Тип функції `Wallet` надається для допомоги у визначенні ваших власних індивідуальних гаманців. Наступні властивості можуть бути налаштовані на вихідному значенні вашої функції `Wallet`:

### Властивості Гаманця

<PropsTable
  data={[
    {
      name: 'id',
      required: true,
      type: 'string',
      description: 'Унікальний ідентифікатор для кожного гаманця.',
    },
    {
      name: 'name',
      required: true,
      type: 'string',
      description: 'Назва гаманця, зрозуміла людині.',
    },
    {
      name: 'rdns',
      required: false,
      type: 'string',
      description: 'RDNS для гаманця, що підтримує EIP6963.',
    },
    {
      name: 'shortName',
      required: false,
      type: 'string | undefined',
      description: 'Необов\'язкова коротка назва для мобільного використання.',
    },
    {
      name: 'iconUrl',
      required: true,
      type: 'string | (() => Promise<string>)',
      description: 'URL для іконки гаманця або обіцянка, що повертає URL даних у форматі Base64.',
    },
    {
      name: 'iconAccent',
      required: false,
      type: 'string',
      description: 'Акцентний колір, який використовується в процесах завантаження для гаманців з розширенням браузера і мобільним додатком.',
    },
    {
      name: 'iconBackground',
      required: true,
      type: 'string',
      description: 'Колір фону під час завантаження іконки гаманця.',
    },
    {
      name: 'installed',
      required: false,
      type: 'boolean | undefined',
      description: (
        <>
          Чи відомо, що гаманець встановлено, або{' '}
          <code>undefined</code>, якщо це невизначено.
        </>
      ),
    },
    {
      name: 'downloadUrls',
      required: false,
      type: '{ android?: string, ios?: string, mobile?: string, qrCode?: string, chrome?: string, firefox?: string, edge?: string, safari?: string, opera?: string, browserExtension?: string } | undefined',
      typeSimple: 'DownloadUrls | undefined',
      description: 'Об\'єкт, що містить URL-адреси для завантаження.',
    },
    {
      name: 'hidden',
      required: false,
      type: '(args: { wallets: Array<{ id: string, connector: Connector, installed?: boolean }> }) => boolean',
      typeSimple: '(args) => boolean | undefined',
      description: 'Функція для обчислення, чи слід приховати цей гаманець зі списку.',
    } 
  ]}
/> Це корисно для визначення загальних резервних гаманців, наприклад, вбудований резервний механізм "Injected Wallet" використовує цю функцію.

### Властивості DownloadUrls

<PropsTable
  data={[
    {
      name: 'android',
      required: false,
      type: 'string',
      description: 'Google Play URL',
    },
    {
      name: 'ios',
      required: false,
      type: 'string',
      description: 'Apple App Store URL',
    },
    {
      name: 'mobile',
      required: false,
      type: 'string',
      description:
        'Landing page for mobile users when `android` or `ios` are unavailable',
    },
    {
      name: 'qrCode',
      required: false,
      type: 'string',
      description:
        'Landing page for users that scan the mobile download QR Code',
    },
    {
      name: 'chrome',
      required: false,
      type: 'string',
      description: 'Chrome Web Store URL',
    },
    {
      name: 'edge',
      required: false,
      type: 'string',
      description: 'Microsoft Edge Add-ons URL',
    },
    {
      name: 'firefox',
      required: false,
      type: 'string',
      description: 'Firefox Browser Add-ons URL',
    },
    {
      name: 'opera',
      required: false,
      type: 'string',
      description: 'Opera add-ons URL',
    },
    {
      name: 'safari',
      required: false,
      type: 'string',
      description: 'Mac App Store URL',
    },
    {
      name: 'browserExtension',
      required: false,
      type: 'string',
      description:
        'Landing page for desktop extension users when browser-compatible URLs are unavailable',
    },
  ]}
/>

### Приклади

Наприклад, щоб створити власний гаманець за допомогою WalletConnect:

```tsx
import { Wallet, getWalletConnectConnector } from '@rainbow-me/rainbowkit';

export interface MyWalletOptions {
  projectId: string;
}

export const rainbow = ({ projectId }: MyWalletOptions): Wallet => ({
  id: 'my-wallet',
  name: 'My Wallet',
  iconUrl: 'https://my-image.xyz',
  iconBackground: '#0c2f78',
  downloadUrls: {
    android: 'https://play.google.com/store/apps/details?id=my.wallet',
    ios: 'https://apps.apple.com/us/app/my-wallet',
    chrome: 'https://chrome.google.com/webstore/detail/my-wallet',
    qrCode: 'https://my-wallet/qr',
  },
  mobile: {
    getUri: (uri: string) => uri,
  },
  qrCode: {
    getUri: (uri: string) => uri,
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend putting My Wallet on your home screen for faster access to your wallet.',
          step: 'install',
          title: 'Open the My Wallet app',
        },
        {
          description:
            'After you scan, a connection prompt will appear for you to connect your wallet.',
          step: 'scan',
          title: 'Tap the scan button',
        },
      ],
    },
  },
  extension: {
    instructions: {
      learnMoreUrl: 'https://my-wallet/learn-more',
      steps: [
        {
          description:
            'We recommend pinning My Wallet to your taskbar for quicker access to your wallet.',
          step: 'install',
          title: 'Install the My Wallet extension',
        },
        {
          description:
            'Be sure to back up your wallet using a secure method. Never share your secret phrase with anyone.',
          step: 'create',
          title: 'Create or Import a Wallet',
        },
        {
          description:
            'Once you set up your wallet, click below to refresh the browser and load up the extension.',
          step: 'refresh',
          title: 'Refresh your browser',
        },
      ],
    },
  },
  createConnector: getWalletConnectConnector({ projectId }),
});
```

Після цього ви можете додати свій власний гаманець у [список гаманців](/docs/custom-wallet-list).
